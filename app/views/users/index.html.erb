<h1>List of Users</h1>
<p>

  <% @users.each do |u| %>
  <div class="grid grid-4">
    <ul>
      <% unless u.id == current_user.id %>

      <li><%= image_tag(u.avatar.thumb.url, alt: 'No Image', class: "friend_avatar") %></li>
      <br>
      <li><%= u.name %></li>
      <br>
      <li>Job: <%= u.job %></li>
      <br>
      <li>Location: <%= u.location %></li>
      <br>

      <% if @friends.any?{|f| f.self_id == u.id || f.friend_id == u.id} %>
      <!-- Thanks Beth! -->
      <li>You and <%= u.name %> are already friends.</li>
      <br>

      <% else %>

      <li><%= link_to("Add #{u.name} as a friend!", friends_path(friend_id: u.id), :method => :post) %></li>

      <% end %>
      <% end %>
    </ul>
  </div>
  
  <% end %>

  <section class="row">
  </section>

</p>